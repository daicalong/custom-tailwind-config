const screens = require('../constants/screens.const');

// This file exports a single preset to be used separately instead of importing the entire theme
module.exports = {
    theme: {
        screens: screens
    }
}